__pycache__/
*.pyc
.DS_Store
.vercel

# Bulk export (regenerated by site/scripts/build-export.mjs; hosted on the
# shared Cloudflare R2 bucket, not committed — see ~/open-corpus/PLAN.md item 8)
# Leading slash anchors this to the repo root — otherwise it would also match
# site/src/pages/export.astro, the Next.js-equivalent route, if it were ever
# named export/ instead of export.astro. Kept anchored anyway for safety.
/export/
